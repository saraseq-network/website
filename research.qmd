---
title: "My Research"
subtitle: "Interested in my ongoing and past research projects? Have a look!"
format:
  html:
    css: styles/research.css
---
<div class="research-intro">
<p>
One of the greatest things about my job is how international and interdisciplinary it can be.  
During my career journey, I was fortunate to work with amazing teams across the globe,  
and with very different livestock production systems.
</p>
  <hr>
</div>


# Global research footprint

```{r, echo=FALSE, message=FALSE, warning=FALSE}

library(dplyr)
library(plotly)
locations <- data.frame(
  city = c("Lisbon, Portugal", "Hong Kong, SAR China", "Columbus, OH, USA"),
  lon = c(-9.139, 114.169, -82.999),  # Longitude
  lat = c(38.722, 22.319, 39.961)     # Latitude
)

geo <- list(
  showland = TRUE,
  showlakes = TRUE,
  showcountries = TRUE,
  showocean = TRUE,
  countrywidth = 0.5,
  landcolor = "lightgrey",
  lakecolor = "white",
  oceancolor = "white",
  projection = list(
    type = 'orthographic',
    rotation = list(lon = -30, lat = 20, roll = 0)  # Adjust rotation for visibility
  ),
  lonaxis = list(showgrid = TRUE, gridcolor = "gray40", gridwidth = 0.5),
  lataxis = list(showgrid = TRUE, gridcolor = "gray40", gridwidth = 0.5)
)

plot_geo() %>%
  add_trace(
    type = "scattergeo",
    mode = "markers+text",
    lon = ~locations$lon,
    lat = ~locations$lat,
    text = ~locations$city,
    textposition = "top center",
    marker = list(size = 8, color = "darkred", symbol = "circle")
  ) %>%
  layout(
    geo = geo
  )
```


## Projects
Below are snapshots of ongoing and past projects.  
Click any card to read more.




